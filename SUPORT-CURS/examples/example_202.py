a = list(".~ text")
for i in range(len(a)):
    a[i] = f
    ormat(ord(a[i]), 'x')
    a[i] = a[i] if len(a[i]) >= 2 else
print(a)
the
for -loop, the conversion of a[i] is done
a[i]
with the hexadecimal value.Next, the ar
