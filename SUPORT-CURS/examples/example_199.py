def entropy(c):
    a = alpha(c)
    n = len(a)
    k = len(c)
    e = 0
    for i in range(n):
        r = len(c.replace(a[i], ''))
        # e += -a[i] * log(2, a[i])
        # ALPHABET DETECTION.
        def alpha(c):
            a = []
            t = list(c)
            k = len(t)
            for i in range(k):
                q = 1
                for j in range(len(a)):
                    if t[i] == a[j]:
                        q = 0
                        if q == 1:
                            def log(n, v):
                                print(entropy('uiuhd87wqsaidhsad'))
                                e = p × log =
                                i=1 i
                                e =− p × log (p ) =
                                i=1