           i = 0
              print("i = " + str(i))
              i += 1
              if i >= 5:
        simulation of “do-while” loop (as Python doe
        loop, a print function is called to display th
        with the string “i=”. Notice that str(i) functio
        fails (i < 5) and break is executed. Note tha