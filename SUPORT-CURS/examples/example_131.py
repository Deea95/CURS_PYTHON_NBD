p = []
n = 3
m = 3
r = ''
for i in range(n + 1):
    p.append([])
    for j in range(m + 1):
        p[i].append(random.randint(0, 9
        r += str(p[i][j]) + ' '
        r += '\n'
        print(r)
        to
        store
        a
        matrix
        of
        random
        numbers.(2)
        A
        the
        number
        of
        rows in the
        matrix.(3)
        Anoth
        number
        of
        columns in the
        matrix.(4)
        An
        em
        the
        matrix.It
        uses
        two
        for loops.The outer lo
        0 ton (inclusive), creating an array p[i] for e
        populates each row (p[i]) with random num
        string r, followed by a space (‘.’), effectively
        row.Once the inner loop completes for each
        (“\n”) to the string r, creating a new line in
