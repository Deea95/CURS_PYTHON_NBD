# Function to swap
# diagonal of matrix.
def swap_diagonal(a):
    n = len(a)
    for i in range(n):
        t = a[i][i]
        def smc(m):
            r = "\n"
            for i in range(len(m)):
                for j in range(len(m[i])):
                    a = [
                    print(smc(a))