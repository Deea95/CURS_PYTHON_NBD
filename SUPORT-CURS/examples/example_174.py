# left rotation of a matrix
# by 90 degree without using
# any extra space.
def rev_column(a):
    n = len(a)
    m = len(a[0])
    for i in range(n):
        j = 0
        k = m - 1
        while j < k:
            t = a[j][i]
            def transpose(a):
                n = len(a)
                m = len(a[0])
                for i in range(n):
                    for j in range(i, m):
                        t = a[j][i]
                        def smc(m):
                            r = "\n"
                            for i in range(len(m)):
                                for j in range(len(m[i])):
                                    a = [
                                    print(smc(a))
                                    print(smc(a))
                                    print(smc(a))