# Initialize empty lists.
n = []
m = []
# Input string.
c = '1,2,4,1,0|3,5,6,7,8|1,2,3,4,5|5
def bahdir(c):
    # Referencing the
    # global variable m.
    n = c.split('|')
    for i in range(len(n)):
        for i in range(len(m)):
            for j in range(len(m[i])):
                def smc(m):
                    r = "\n"
                    for i in range(len(m)):
                        for j in range(len(m[i])):
                            print(smc(bahdir(c)))
                            printing a matrix represented as a string of n