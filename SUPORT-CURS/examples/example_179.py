            def f_nor(a, b):
              return f_not(f_or(a, b))
            def f_not(a):
            def f_or(a, b):
              return (a + b) - (a * b)
            result = f_nor(0, 0)
            print(f'[0, 0] -> {result}')
        The NOR gate returns a true (1) output onl
        (0). The code begins with a comment block
        Q. The print function is used to display the
        combination of A and B. For example, [0, 0]
        The novel function called f nor(a, b) takes t
        one of the input values (a or b) is 1; otherwis
        the NOR gate output for the input combinati
        0] → 1).