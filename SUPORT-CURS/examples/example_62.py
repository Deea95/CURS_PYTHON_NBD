a = [3, 3, 4, 2, 9, 8, 3]
l = len(a)
min_val = a[0]
for k in range(0, l):
    if a[k] < min_val:
        min_val = a[k]
print(min_val)
the
range
for looping through the array ele
the
loop, there is an if statement
that
check
current
minimum
value
min
val.If
a[k] is ind
val is updated
to
a[k], effectively
finding
the
the
loop, the
code
prints
the
minimum
value
