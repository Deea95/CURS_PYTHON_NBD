            def f_nand(a, b):
              return f_not(f_and(a, b))
            def f_not(a):
            def f_and(a, b):
            print('[1, 1] -> ' + str(f_nand(1, 1
        b), f not(a) and f and(a, b). The f nand(a,
        f not(a) function implements the NOT gate
        and(a, b) function implements the AND gate
        gate by calling f nand(1, 1).