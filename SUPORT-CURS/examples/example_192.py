a = []
def permute(s, r, l):
    if l == r:
    else:
        for i in range(l, r + 1):
            s = swap(s, l, i)
            s = swap(s, l, i)
            def swap(s, i, j):
                c = list(s)
                t = c[i]
                s = "ACTG"
                n = len(s)
                print(a)