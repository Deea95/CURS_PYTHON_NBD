def sum_array(n, q, r):
    if n <= 0:
    else:
        q = [1, 3, 3, 4, 5, 9]
        f = sum_array(len(q) - 1, q, 0)
        print(f"Sum array: [{f}]")
        prints a message to the console that includes