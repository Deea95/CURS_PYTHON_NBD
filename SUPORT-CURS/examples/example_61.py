           a = [2, 3, 4, 5, 9, 8, 3]
           l = len(a)
           max_val = 0
           for k in range(l):
              if a[k] > max_val:
                max_val = a[k]
           print(max_val)
        3]. It then calculates the length of this arra
        each iteration of the loop, there is an if state
        the max val variable with the value of a[k]