           a = 0
           for i in range(1, 6):
              a = a + (i + 4 * 3)
           print(a)
        there is a for-loop that runs 5 times (remem
        inclusive). Within each iteration of the loo
        increments by 1 each time) is added to the pr
        (which is 4 multiplied by 3) is added to the
        the loop completes its 5 iterations, the print