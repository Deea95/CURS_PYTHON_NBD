m = [
    [2, 4, 4],
    [3, 5, 6],
    [3, 5, 4]
]
c = []
for i in range(len(m)):
    for j in range(len(m[i])):
        if len(c) <= j:
            c.append(0)
        c[j] += m[i][j]
        # c[j] *= m[i][j]
print(c)
exist(len(c) <= j), it
initializes
that
element
adds
the
value
of
m[i][j]
to
the
correspondin
operation
c[j] *= m[i][j], indicating
that
any
ends
with a print statement that allows the co
