def for_loop(a, b, r):
    a += 1
    r += 5
    if a >= b:
    else:
        return for_loop(a, b, r)


a = for_loop(0, 7, 0)
print(a)
ment
for traditional repeat loops.The variab
function
checks if a is greater
than or equal
the
for loop function is stored in the variable
