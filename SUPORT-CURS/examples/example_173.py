# Function to transpose a matrix.
def transpose(a):
    n = len(a)
    m = len(a[0])
    for i in range(n):
        for j in range(i, m):
            t = a[j][i]
            def smc(m):
                r = "\n"
                for i in range(len(m)):
                    for j in range(len(m[i])):
                        a = [
                        print(smc(a))
                        printed using a print function. The transpose