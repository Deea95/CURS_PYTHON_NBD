class Obj:
    def __init__(self):
        def dx(self, a):
            n = len(a)
            b = 0
            e = 0
            for j in a:
                x = b / n
                for j in a:
                    s = (e / (n - 1)) ** 0.5
                    c = s / x
                    # Creating an instance of Obj
                    # and applying the dx function.
                    obj = Obj()
                    a = [5, 6, 2, 9, 44, 200]
                    print('AV:', obj.AV)
                    print('SD:', obj.SD)
                    print('CV:', obj.CV)