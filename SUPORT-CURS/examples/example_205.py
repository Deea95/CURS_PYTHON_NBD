def convert_to(h, a):
    n = len(a)
    t = sum(len(element) for element
            if t == 1:
    q = 'txt'
    elif 2 <= t < 3:
    q = 'hex'
    elif 3 <= t < 4:
    q = 'dec'
    else:
    q = 'bin'
    if q == h:
        for
    i in range(len(a)):
    conversion_type = q + '_' + h
    if conversion_type == 'txt_he
    a[i] = f
    ormat(ord(a[i]),
    elif conversion_type == 'dec_
    a[i] = f
    ormat(chr(a[i]),
    elif conversion_type == 'txt_
    a[i] = f
    ormat(ord(a[i]),
    elif conversion_type == 'txt_
    a[i] = o
    rd(a[i])
    elif conversion_type == 'hex_
    a[i] = o
    rd(chr(int(a[i],
    elif conversion_type == 'bin_
    a[i] = o
    rd(chr(int(a[i],
    elif conversion_type == 'hex_
    a[i] = c
    hr(int(a[i], 16))
    elif conversion_type == 'bin_
    a[i] = c
    hr(int(a[i], 2))
    elif conversion_type == 'dec_
    a[i] = c
    hr(a[i])
    elif conversion_type == 'dec_
    a[i] = format(ord(chr(a[i
    elif conversion_type == 'hex_
    a[i] = format(int(a[i], 1
    elif conversion_type == 'bin_
    a[i] = format(int(a[i], 2
    a = list("☁Ѐ.~ text")
    print('Array a =', a)
    print(convert_to('bin', a))
    Array
    a = ☁, Ѐ,., ~,, t, e, x, t
    Separate
    functions
    for one to one conversi
    the original array a for user inspection and
    tion called convert to(h, a).This function ta
    target conversion type (e.g., “bin”, “hex”, “d
    this average length.It then checks if the targe
