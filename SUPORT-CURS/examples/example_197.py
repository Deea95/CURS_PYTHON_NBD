def smc(m):
    r = ''
    for row in m:
        for val in row:
            r += val
        r += "\n"


a = [5, 2, 8, 4, 6, 12, 8, 9]
m = 10
n = len(a)
max_value = max(a)
t = [['' for _ in range(n)] for _ in
     for j in range(m):
for i in range(n):
    f = (m * a[i]) // max_value
t[m - j - 1][i] = '\u2591'
if j < f:
    t[m - j - 1][i] = '\u2593'
print(smc(t))
mation
Interchange) / UTF - 8
chart
can
be
m
a[i], and then
it
assigns
the
corresponding
ch
