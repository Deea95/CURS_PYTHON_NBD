def p(a):
    max_value = max(a)
    n = len(a)
    m = 100
    t = [''] * n
    for i in range(n):
        t[i] = str(round((m / max_value
                          a =[5, 1, 8, 4, 6, 2, 9, 8]
                          print(p(a))


def p(a):
    max_value = max(a)
    n = len(a)
    m = 100
    t = []
    for i in range(n):
        t.append(str(round((m / max_val
                            a =[5, 1, 8, 4, 6, 2, 9, 8]
                            print(p(a))

