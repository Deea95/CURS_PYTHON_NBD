           r = 0
           for i in range(10):
              for j in range(10):
                r += j * i
           print(r)
        a variable j which also runs from 0 to 9 (
        upper limit is non-inclusive). For each comb
        accumulated total in r is then printed out. T