m = [
    [2, 4, 6],
    [3, 5, 6],
    [3, 5, 4]
]
r = 0
for i in range(len(m)):
    for j in range(len(m[i])):
        r += m[i][j]
print(r)
in the
variable
r.It
uses
nested
for loops to
    it
    will
    go
    through
    three
    rows
    since
    len(m) is
    elements
    within
    each
    row
    using
    len(m[i]).F
    the
    script
    prints
    out
    the
    sum
    r
    after
    the
    loops
