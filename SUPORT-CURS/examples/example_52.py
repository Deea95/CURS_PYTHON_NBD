A = ["a", "b", "c", "d", "e"]
t = ""
for i in range(len(A)):
    t += "\n A[" + str(i) + "]=" + A[
        print(t)
    appends
    a
    newline
    character(“\n”) to
    the
    t
    st
    with the current value of i, followed by “]=
loop
finishes, the
implementation
prints
the
values, and then
prints
the
string.
