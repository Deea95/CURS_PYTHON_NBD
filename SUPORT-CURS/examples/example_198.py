a = [0] * 9
n = 9
for k in range(n):
    def chart(a):
        m = 9
        t = [['' for _ in range(n)] for _
        # Use default=1 to avoid
        # division by zero.
        max_val = max(a, default=1)
        for j in range(m):
            for i in range(n):
                f = int((m / max_val) * a
                if j < f:
                    def SMC(a):
                        n = len(a)
                        m = len(a[0])
                        r = ''
                        for i in range(n + 2):
                            for j in range(m):
                                if i < n:
                                    if i == n:
                                        if i > n:
                                            if i == n:
                                                if i < n:
                                                    print(SMC(chart(a)))
                                                    print('\n' + str(a))