            def f_or(a, b):
              return (a + b) - (a * b)
            print(f'[1, 0] -> {f_or(1, 0)}')
        logical OR operation for two binary inputs A
        and 1 for A and B) and the corresponding