           a = 1
           b = 2
           c = 3
           if a < b:
              c += 1
           else:
              c -= 1
           print("c=" + str(c))
        if-else conditional statement that checks if th
        is not less than b), the value of c is decreme
        statement, the code prints to the console th