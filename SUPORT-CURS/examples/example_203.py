def hexify(a):
    for i in range(len(a)):
        a[i] = format(ord(a[i]), 'x')
        a[i] = a[i] if len(a[i]) >= 2
        els


a = list(".~ text")
print(hexify(a))
characters.Then, it
calls
the
hexify(a)
functi
the
result
to
the
console
for user inspection.
