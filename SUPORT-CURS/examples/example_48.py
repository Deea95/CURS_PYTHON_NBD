           a = [5, 6, 8]
           b = len(a)
           print(b)
        len(a) is a property of the array a, which rep
        Next, the value of b is printed in the output
        determines its length, and prints that length t