          def compute(x):
          a = 10
          b = compute(a)
          print(b)
                          compute(x) =
                         compute(x) =
                          compute(x) =
        function call, “compute(a)”. The compute fun
        operations. Next, the value of b is printed to