def f_xor(a, b):
    return (a + b) - 2 * (a * b)
    # return (a - b) * (a - b)
    # return ((a + b) * (a + b)) % 2


print('[0, 0] ->', f_xor(0, 0))
the
truth
table
for XOR, showing the input va
and prints
the
result
to
the
console.The
f
xo
b), which
can
be
0, 1, or 2.
Then, subtracting
