def sequence(n, m, i, t):
    m.append(n)
    i += 1
    if i >= t:
    else:
        return sequence((n - 1) + (n - 2),
                        # and printing the sequence.
                        d=sequence(5, [], 0, 5)
        print("A sequence:\n[", d, "]")
        string is printed
        to
        the
        console, which
        include
        result is then
        printed in the
        output.
