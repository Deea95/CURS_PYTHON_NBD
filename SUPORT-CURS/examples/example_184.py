            # greatest common divisor (GCD).
            def gcd(a, b):
              if a == 0:
              while b != 0:
                if a > b:
                   a -= b
                else:
                   b -= a
            print(gcd(45, 12))